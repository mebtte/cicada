import { createTheme } from '@mui/material';

export default createTheme({
  palette: {
    primary: {
      main: 'rgb(49, 194, 124)',
      contrastText: 'rgb(255, 255, 255)',
    },
  },
  typography: {
    fontSize: 12,
  },
});
